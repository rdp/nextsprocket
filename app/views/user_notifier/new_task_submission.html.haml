:plain
  #{@ts.profile.name} just submitted a solution for #{@ts.task.title}.
    
  You can view #{@ts.profile.name}'s submission here:
  #{task_submission_url(@ts.task, @ts)}

  And the task page here:
  #{task_url(@ts.task)}

  Thanks,
  The NextSprocket Team
  -------
  Get updates and chat with the team,
  Follow us on Twitter!
  http://twitter.com/nextsprocket
  